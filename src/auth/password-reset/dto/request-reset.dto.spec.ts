
import { RequestPasswordResetDto } from './request-reset.dto';

describe('RequestPasswordResetDto', () => {
  it('should be defined', () => {
    expect(new RequestPasswordResetDto()).toBeDefined();
  });
});
